import { type ThemeOptions } from '@exotel-npm-dev/signal-design-system';

export const theme: ThemeOptions = {
  colorSchemes: {
    light: {
      palette: {
        custom: {
          loginBgColor: '#fffaeb',
        },
      },
    },
    dark: {
      palette: {
        custom: {
          loginBgColor: '#232323',
        },
      },
    },
  }
};
